package cmd

import (
    "context"
	"fmt"

    "github.com/ignite/cli/v28/ignite/services/app"
)

// ExecuteHello executes the hello subcommand.
func ExecuteHello(ctx context.Context, cmd *app.ExecutedCommand) error {
    fmt.Println("Hello, world!")
    return nil
}
