package cmd

import "github.com/ignite/cli/v28/ignite/services/app"

// GetCommands returns the list of <%= Name %> app commands.
func GetCommands() []*app.Command {
	return []*app.Command{
		{
			Use:     "<%= Name %> [command]",
			Short:   "<%= Name %> is an awesome Ignite application!",
			Commands: []*app.Command{
				{
					Use:     "hello",
					Short:   "Say hello to the world of ignite!",
				},
			},
		},
	}
}
